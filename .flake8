[flake8]
exclude =
	# don't traverse git directory
    .git,
    # don't traverse frontend
    frontend,
    # don't traverse cached files
    __pycache__,
    # don't traverse venv files
    bin,
    lib,
    share,
    # don't traverse autogenerated scripts
    migrations
max-line-length = 99